# This site only redirects to https://blog.geeknite.com/ -- see README.md.
#
# Everything must stay crawlable: index.html and 404.html ARE the redirect, and a
# crawler that cannot fetch them cannot learn the blog moved. The repository holds
# nothing else -- its content was removed from main on 2026-09-16 and lives in the
# git history only.
#
# No Sitemap line on purpose. There is no sitemap on this host; the blog publishes
# its own at https://blog.geeknite.com/sitemap.xml. The previous version of this
# file declared "Sitemap: {{ site.url }}/sitemap.xml" and served it as that literal
# text, because .nojekyll means nothing renders Liquid here.

User-agent: *
Allow: /
